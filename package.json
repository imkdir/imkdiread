{
  "name": "index",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "npm install --prefix frontend && npm install --prefix backend",
    "dev": "bash scripts/dev.sh",
    "start": "npm run start --prefix backend",
    "build": "concurrently \"npm run build --prefix frontend\" \"npm run build --prefix backend --if-present\"",
    "test": "npm run test:smoke --prefix backend && npm run lint --prefix frontend",
    "deploy": "bash scripts/deploy.sh",
    "backup:db": "bash scripts/sync-vps-database.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.3.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "uuid": "^13.0.0"
  }
}
